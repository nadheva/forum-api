name: Continuous Integration

on:
  pull_request:
    branches:
      - main
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:14
        env:
          # Gunakan nilai hardcoded untuk DB sementara di CI
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: supersecretpassword
          POSTGRES_DB: forumapi_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install, Migrate and Test
        run: |
          npm install
          npm run migrate up
          npm run test
        env:
          CI: true
          # Ini kunci perbaikannya: Pastikan string password terbaca dengan benar
          DATABASE_URL: postgres://postgres:supersecretpassword@localhost:5432/forumapi_test
          
          # Variabel satuan untuk aplikasi (Sesuai dengan yang dipanggil di Infrastructure)
          PGHOST_TEST: localhost
          PGUSER_TEST: postgres
          PGDATABASE_TEST: forumapi_test
          PGPASSWORD_TEST: supersecretpassword
          PGPORT_TEST: 5432
          
          # Variabel standar (opsional untuk jaga-jaga)
          PGHOST: localhost
          PGUSER: postgres
          PGDATABASE: forumapi_test
          PGPASSWORD: supersecretpassword
          PGPORT: 5432
          
          # Gunakan Secrets untuk Key Keamanan
          ACCESS_TOKEN_KEY: ${{ secrets.ACCESS_TOKEN_KEY }}
          REFRESH_TOKEN_KEY: ${{ secrets.REFRESH_TOKEN_KEY }}
          ACCESS_TOKEN_AGE: 1800